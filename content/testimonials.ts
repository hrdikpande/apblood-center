import type { Testimonial } from "./types";

// CLIENT-VERIFIED DATA REQUIRED — do not populate with invented quotes, names,
// locations or star ratings. Only add entries here once the client has
// supplied written consent and verified patient testimonials. The
// TestimonialsSection component renders a "coming soon" empty state and
// emits no review/aggregateRating schema while this array is empty.
export const testimonials: Testimonial[] = [];
